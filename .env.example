# Monday.com Credentials (Optional - system falls back to seed dataset if empty)
MONDAY_API_TOKEN=your_monday_api_v2_token_here
MONDAY_DEALS_BOARD_ID=
MONDAY_WORK_ORDERS_BOARD_ID=

# LLM Configuration (Optional - fallback rule engine active if empty)
LLM_PROVIDER=openai
OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini

# Application Settings
PORT=8000
ENVIRONMENT=development
LOG_LEVEL=INFO
